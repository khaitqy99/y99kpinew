// Script này đã được loại bỏ vì không còn bảng companies
// File này giữ lại để tránh lỗi import nếu có file khác import nó

export async function ensureDefaultCompany() {
  console.log('⚠️ Script ensureDefaultCompany đã được loại bỏ vì không còn bảng companies');
  return null;
}
